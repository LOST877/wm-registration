---
name: Правила для AI-агентов (Wheel Masters)
---

**Обновлено:** 2026-04-05 | **Версия:** v1.0  
**Проект:** [Wheel Masters](./PROJECT_KNOLEDGE.md) — публичный сайт для кросс-кантри гонок.

---

## 🎯 Общие принципы

1. **Сначала читай существующий код и документацию.**  
   Перед любыми изменениями — изучи `PROJECT_KNOLEDGE.md`, структуру папок, API и БД.

2. **Пиши в стиле проекта:**
   - PHP: **чистый, процедурно-объектный**, без фреймворков (OO + PDO)
   - JS: ES6+, без зависимостей, понятные имена функций
   - HTML/CSS: семантический HTML5 + inline-стили только при крайней необходимости

3. **Строго избегай изменений:**
   - Безопасности (`password_hash`, `PDO::prepare`, HTTPS, проверки CSRF/уникальности)
   - Формата ФИО и города (`ucfirst(strtolower(...))`)
   - Структуры БД без предварительного согласования

---

## 🧩 Работа с API и данными

4. **Проверяй наличие и тип данных перед обработкой.**  
   Пример (PHP):
   ```php
   if (!isset($_GET['race_id']) || !is_numeric($_GET['race_id'])) {
       http_response_code(400);
       echo json_encode(['error' => 'Missing or invalid race_id']);
       exit;
   }
   ```

5. **Дубли всегда проверяй по `(race_id, phone, first_name, last_name)`**, не только по телефону.

6. **Перед вставкой в БД — форматируй строки на сервере:**
   ```php
   $firstName = ucfirst(strtolower($_POST['firstName']));
   $lastName  = ucfirst(strtolower($_POST['lastName']));
   $city      = ucfirst(strtolower($_POST['city']));
   ```

7. **Каждый ответ API — JSON.**
   - `200 OK`: `{"success": true, "message": "...", "id": 42}`  
   - `409 Conflict`: `{"success": false, "error": "Duplicate registration"}`  
   - `400`: `{"success": false, "error": "Invalid race_id"}`  
   - `404`: `{"success": false, "error": "Race not found"}`  
   - `500`: `{"error": "Database error"}`

8. **`api/race.php` — НЕ возвращает категории.**  
   Категории загружаются отдельно: `api/categories.php?race_id={id}`.  
   Не пиши код, который ожидает `race.categories` от `api/race.php`.

---

## 🔐 Безопасность — не обсуждается

9. **Всегда используй `PDO::prepare()` для любых SQL-запросов.**

10. **Проверяй CSRF-токен в `POST`-запросах (в `api/register.php` и админке) с помощью `hash_equals()`.**  
   Пример:
   ```php
   session_start();
   if (!isset($_POST['csrf_token']) || !hash_equals($_SESSION['csrf_token'] ?? '', $_POST['csrf_token'])) {
       http_response_code(403);
       echo json_encode(['error' => 'Invalid CSRF token']);
       exit;
   }
   ```
   *(Для генерации токена в `index.html`: добавь `<script>document.querySelector('form').addEventListener('submit', e => { ... })</script>` или подобный клиентский JS. Но лучше — через `<meta name="csrf_token" content="...">` и JS-вставку в форму.)*

11. **Никогда не используй `$_POST`, `$_GET`, `$_REQUEST` напрямую в SQL — только через prepare.**

---

## 📦 Файлы и структура

12. **Пиши код в соответствии с дерикторией:**
   - `api/*.php` — только API-логика (REST + PDO), **без HTML**
   - `script.js` — логика HTML-страницы (загрузка гонки, форма, список участников)
   - `style.css` — только CSS (без JS и HTML)
   - `assets/` — только файлы (`*.webp`, `*.png`, `*.jpg`)
   - `admin/`, `api/admin/` — админка (работает полностью, не ломай без тестирования)

13. **Не добавляй новых папок без согласования.**

---

## 📚 Комментарии и документация

14. **Комментируй сложные или нетривиальные участки.**
   Пример:
   ```php
   // Форматирование ФИО — чтобы дубли по разному написанному ФИО не пропускались
   $firstName = ucfirst(strtolower($_POST['firstName']));
   ```

15. **При добавлении API-эндпоинта — обнови `PROJECT_KNOLEDGE.md` (даже черновик).**

---

## 🧪 Тестирование

16. **Перед отправкой — проверь на Open Server Panel:**
   - Отправка формы → ошибка 409 (если дубль)  
   - Несуществующая `race_id` → 404  
   - Пустой `firstName`/`lastName` → 200 OK или 400? (Согласовано: **принимать, но не сохранять**, если `NULL` — пока оставляем как есть, так как на фронте валидация)

---

## ⏳ Известные ограничения (обязательно знать!)

17. **Админка — готова и работает. Не ломай существующую логику.**  
    ✅ Админка работает полностью (`admin/index.html`, `api/admin/*.php`, `_auth.php`).  
    Любые изменения — только после тестирования и уведомления.

18. **CSRF-защита — запланирована.**  
    При внедрении используй `hash_equals()` и `$_SESSION['csrf_token']`.

19. **email/team не обязательны — не делай их обязательными в формах.**  
    ✅ Верно — оставляем.

20. **Переименование категории — заблокировано, если используется в гонках.**

21. **Изменения в `api/race.php`:**  
    - Не добавляй категории в ответ — они грузятся отдельно.  
    - Если нужен объединённый эндпоинт — сделай `api/race-full.php` и обнови `PROJECT_KNOLEDGE.md`.

---

## ✅ Завершение работы

22. **Всегда вызывай `exit` после `json_encode()` или `http_response_code()` в API, чтобы избежать HTML-вывода.**

23. **После изменений — проверь, что `script.js` не ломает `form.submit()` и `fetch(...).then()`**

24. **Сообщай, если изменил `PROJECT_KNOLEDGE.md` или поведение API — пусть следующий агент знает что изменилось.**